# Edge cases — html-output

Factual exceptions accumulated from real runs. Likely categories:
- Specific calling skills that need a custom output filename pattern
- File size limits for downstream consumers (e.g. Slack 1MB cap)
- Browsers / environments where a CDN library fails (offline review, corporate proxy)
- Render-target quirks (LinkedIn-shareable HTML must inline everything; Notion paste eats certain tags)

_(empty — populate as encountered)_
