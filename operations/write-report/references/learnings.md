# Learnings — write-report

Behavioural feedback accumulated through use. Each entry: date, observation, applied change (or none yet).

Starts empty.

## What Has Worked

_(populate as patterns prove reliable)_

## What Has Failed

_(populate as patterns prove unreliable)_

## Patterns and Preferences

_(populate as the owner expresses standing preferences across runs)_

## Open Questions

- How aggressive should the calling skill be with D3 (specificity) fixes? Some abstractions are legitimately the right level of abstraction. The current rule is "flag only when a named specific is available nearby" — verify this holds in practice.
- Should there be a strictness level between `low` and `standard` for short memos (500-1000 words)? Current bucketing may over- or under-fire.
- Should `wiki` doc_type get its own readability rubric variant? Wiki pages legitimately use noun-label headings but still benefit from so-what discipline in body prose.
