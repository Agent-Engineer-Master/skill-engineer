# Learnings — auto-research

Consolidate at 80 lines. Hard cap at 100. Promote entries reinforced 2+ times to SKILL.md rules.

---

## What Has Worked

- Starting with Level 1 (deterministic) criteria first, then adding Level 2 once L1 is at 100%, reduces confounding — one variable type at a time — [initial design]

## What Has Failed

- Advisory language ("sweet spot", "try to avoid") does not hold against generative pressure — always use imperative hard limits with an explicit action when the behaviour must be guaranteed
- A rule buried in a bullet list will be skipped; rules that must never slip need (a) bold callout at the drafting step, (b) explicit checklist row at self-critique, and (c) a hard gate blocking progression to the next step

## Patterns and Preferences

- (no entries yet)

## Open Questions

- Whether a train/val split is worth the overhead for skills with fewer than 10 test inputs — watch for overfitting signals in early runs
- How to handle Level 3 (voice/creativity) properties that resist decomposition — current approach: note as open question rather than force as criterion
